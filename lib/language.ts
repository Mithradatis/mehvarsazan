import type Language from "@/types/language";

const languages: Language = {
    en: { name: 'English', abbreviation: 'EN', dir: 'ltr', locale: 'en_US' },
    fa: { name: 'فارسی', abbreviation: 'فا', dir: 'rtl', locale: 'fa_IR' }
};

export default languages;